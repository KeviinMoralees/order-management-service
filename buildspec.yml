version: 0.2

phases:
  install:
    commands:
      - echo "Usando Go desde la imagen de CodeBuild:"
      - go version

  build:
    commands:
      - echo "Compilando aplicación Go..."
      - go build -o hello-world .

  post_build:
    commands:
      - echo "Ejecutando binario para ver el Hello World:"
      - ./hello-world

artifacts:
  files:
    - hello-world

